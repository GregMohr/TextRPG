namespace TextRPG
{
    public class TestMenu
    {
        public TestMenu(){

        }
        public static void Test(){
            // Menu townMenu = new Menu("You are in a town. You can...");
            // townMenu.Add(new MenuEntry("1 - Adventure | ", Program.Encounter));
            // townMenu.Generate();
            // Console.WriteLine(player.name + "(" + player.health + "), Please select action:");
            // Menu playerActionMenu = new Menu("Jeb(100), Please select action:");//probably leave menu header gen where it already is
            // playerActionMenu.Add(new PlayerAction("1 - Basic Attack | ", this.Attack));
            // playerActionMenu.Add(new PlayerAction("2 - Death Blow | ", this.DeathBlow));
            // playerActionMenu.Add(new PlayerAction("3 - Meditate", this.Meditate));      
        }
    }
}